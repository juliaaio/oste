org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=true
# Disable Gradle daemon to prevent incremental cache file locking issues
org.gradle.daemon=false
# Disable Kotlin incremental compilation to fix .tab file locking conflict
kotlin.incremental=false
# Use in-process Kotlin compiler to avoid daemon file access issues
kotlin.compiler.execution.strategy=in-process
